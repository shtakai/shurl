version: '2'
services:
  db:
    image: postgres
  web:
    env_file: .env
    build: .
    command: bundle exec rails server --environment production -p 3000 -b '0.0.0.0'
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db
